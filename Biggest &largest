#include <stdio.h>

int main() 

{
    int a,b,c,d;
    printf("enter the number");
    scanf("%d%d%d%d",&a,&b,&c,&d);
    int big= a>b&&b>c&&c>d? a:(b>c&&c>d)? b:(c>d)? c:d ;
    int small=a<b&&b<c&&c<d? c:(b<c&&c<d)? c:(c<d)? c:d;
    printf("%d",small+big);
    return 0;
}
